@model IEnumerable<Gadzet.Models.CMS.Aktualnosc>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tytul)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tresc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pozycja)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tytul)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tresc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pozycja)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAktualnosc }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAktualnosc }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAktualnosc })
        </td>
    </tr>
}

</table>
